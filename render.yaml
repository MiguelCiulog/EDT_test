services:
  # A Docker web service
  - type: web
    name: EDT_test_fastapi
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $SERVER_PORT --app-dir ./src/edt/
